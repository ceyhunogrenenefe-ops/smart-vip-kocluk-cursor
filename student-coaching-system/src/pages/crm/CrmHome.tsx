import MeetingTrackerPage from '../MeetingTrackerPage';

/** /crm index: pipeline (admin ve temsilciler). Yalnız temsilcinin açılışı HomeRedirect ile inbox’tır. */
export default function CrmHome() {
  return <MeetingTrackerPage />;
}
